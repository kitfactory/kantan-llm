# kantan-llm plan（v0.1）

チェックしながら進める（Doneは `[x]`）。

- [x] `docs/concept.md` / `docs/spec.md` / `docs/architecture.md` を要件に合わせて作成
- [x] `get_llm`（推測/上書き/フォールバック）を実装
- [x] 環境変数の仕様をREADMEに明記
- [x] 例外（Error ID）を実装し、READMEの失敗例と文言一致
- [x] 最小テスト（import / 分岐 / 不足エラー）を追加
- [x] `pytest` を通す
